#include "main.h"
/**
 * _abs - That computes the absolute value of an integer
 *
 * @n: number
 *
 * Return: Always
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
