#include "main.h"

/**
 * print_sign - print the singler number
 * @n: The print to int
 * Return: Always
 */

int print_sign(int n)
{
int n;
if (n > 0)
_putchar('+');
return (1);
else if (n == 0)
_putchar('0');
return (0);

else
(n < 0)
_putchar('-');
return (-1);
}
